# Environment Configuration
# Copy this file to .env and update values
# ========================================
# Khi deploy lên server, thay localhost thành PUBLIC_IP hoặc domain
# ========================================

# ===========================================
# URL Configuration (QUAN TRỌNG - THAY ĐỔI KHI DEPLOY)
# ===========================================

# INTERNAL URL: FE gọi BE qua localhost (tốc độ cao, không qua Kong)
# - Dùng cho API calls từ FE -> BE
# - Giữ nguyên localhost để tối ưu tốc độ
INTERNAL_BACKEND_HOST=localhost
INTERNAL_BACKEND_PORT=8999

# PUBLIC URL: URL public qua Kong (để hiển thị ảnh, static files)
# - Dùng để hiển thị ảnh trên FE
# - Thay đổi thành domain/IP public khi deploy
# - Ví dụ: https://api.yourdomain.com hoặc http://123.456.789.10:8999
PUBLIC_BACKEND_URL=http://localhost:8999

# Frontend URL (để CORS)
FRONTEND_HOST=localhost
FRONTEND_PORT=3000

# ===========================================
# App
# ===========================================
APP_NAME=Medical Imaging System
APP_VERSION=1.0.0
DEBUG=true
API_V1_PREFIX=/api/v1/ai_medical

# ===========================================
# Security
# ===========================================
SECRET_KEY=your-super-secret-key-change-in-production-123456789
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7

# ===========================================
# Database (PostgreSQL)
# ===========================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-password
POSTGRES_DB=medical_imaging
# DATABASE_URL sẽ được tự động generate từ các biến trên nếu không set
# DATABASE_URL=postgresql://postgres:password@localhost:5432/medical_imaging

# ===========================================
# Redis
# ===========================================
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_URL sẽ được tự động generate từ các biến trên nếu không set
# REDIS_URL=redis://localhost:6379/0

# ===========================================
# RabbitMQ
# ===========================================
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5670
RABBITMQ_USER=user
RABBITMQ_PASSWORD=adminace123
RABBITMQ_VHOST=/

# ===========================================
# CORS - Tự động generate từ FRONTEND_HOST và FRONTEND_PORT
# ===========================================
# CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# ===========================================
# File Storage
# ===========================================
STATIC_DIR=static
PATIENT_FILES_DIR=patient_files
FACE_IMAGES_DIR=face_images

# ===========================================
# Directories (relative to BASE_DIR)
# ===========================================
DATASET_DIR=../dataset_LIDC_IDRI_filtered
DIFFDRR_DIR=DoAnPtit_DiffDrr
CYCLEGAN_DIR=DoAnPtit_CycleGan
XRAY2CT_DIR=DoAnPtit_Xray2CT
RESULTS_DIR=results

# ===========================================
# Inference Model
# ===========================================
MODEL_CHECKPOINT=DoAnPtit_Xray2CT/checkpoints/model-81.pt
