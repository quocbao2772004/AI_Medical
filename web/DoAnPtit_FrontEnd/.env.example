# Frontend Environment Configuration
# Copy this file to .env and update values as needed
# ========================================
# Khi deploy lên server, thay localhost thành PUBLIC_IP hoặc domain
# ========================================

# ===========================================
# URL Configuration (QUAN TRỌNG - THAY ĐỔI KHI DEPLOY)
# ===========================================

# INTERNAL URL: FE gọi BE qua localhost (tốc độ cao, không qua Kong)
# - Dùng cho API calls từ FE -> BE
# - Giữ nguyên localhost để tối ưu tốc độ
REACT_APP_INTERNAL_BACKEND_HOST=localhost
REACT_APP_INTERNAL_BACKEND_PORT=8999

# PUBLIC URL: URL public qua Kong (để hiển thị ảnh, static files)
# - Dùng để hiển thị ảnh trên FE
# - Thay đổi thành domain/IP public khi deploy
# - Ví dụ: https://api.yourdomain.com hoặc http://123.456.789.10:8999
REACT_APP_PUBLIC_BACKEND_URL=http://localhost:8999

# ===========================================
# API Configuration (TÙY CHỌN - nếu không set sẽ tự động generate từ HOST)
# ===========================================

# Backend API URL (without trailing slash)
# REACT_APP_INTERNAL_API_URL=http://localhost:8999/api/v1/ai_medical

# Socket.IO Server URL (without trailing slash)
# REACT_APP_INTERNAL_SOCKET_URL=http://localhost:8999

# ===========================================
# App Configuration
# ===========================================

# App Name (displayed in UI)
REACT_APP_NAME=Medical Imaging System

# App Version
REACT_APP_VERSION=1.0.0

# ===========================================
# Feature Flags
# ===========================================

# Enable/disable debug mode (shows console logs)
REACT_APP_DEBUG=true

# Enable/disable Socket.IO real-time notifications
REACT_APP_ENABLE_SOCKET=true

# ===========================================
# File Upload Configuration
# ===========================================

# Maximum file size for X-ray upload (in MB)
REACT_APP_MAX_UPLOAD_SIZE_MB=50

# Allowed file types for X-ray upload
REACT_APP_ALLOWED_FILE_TYPES=.png,.jpg,.jpeg,.npy

# ===========================================
# UI Configuration
# ===========================================

# Default language (vi | en)
REACT_APP_DEFAULT_LANGUAGE=vi

# Items per page in tables
REACT_APP_DEFAULT_PAGE_SIZE=10
